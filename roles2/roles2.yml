---
  - name: build configuration
    hosts: buildapp
    become: yes

    roles:
      - common
      - build
  
  - name: prod config
    hosts: prod
    become: yes

    roles:
      - common
      - prod